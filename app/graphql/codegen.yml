schema: "${GRAPHCMS_ENDPOINT}"
documents: app/graphql/query.gql
generates:
  app/graphql/generated.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-graphql-request
