query Posts {
  posts {
    title
    image
    movie
    createdAt
  }
}

query Post($movie: String!) {
  post(where: { movie: $movie }) {
    title
    description
    movie
    createdAt
    image
  }
}
